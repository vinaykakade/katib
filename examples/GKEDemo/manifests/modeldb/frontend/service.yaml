apiVersion: v1
kind: Service
metadata:
  name: modeldb-frontend
  namespace: katib
  labels:
    app: modeldb
    component: frontend
spec:
  ports:
    - port: 443
      targetPort: 3000
      protocol: TCP
      name: ui
  selector:
    app: modeldb
    component: frontend
